// This file is auto-generated by sdk.js. Do not edit manually.

export function addAlert(message, user, messageId) {
  const elem = document.createElement('div');
  elem.innerHTML = `
<div class="alert" id="m${messageId}">
    <div>${user}</div>
    <span>${message}</span>
</div>
`;
  const container = document.getElementById('main-container');
  if (container) {
    container.appendChild(elem);
  } else {
    console.error("#main-container not found in the DOM.");
  }
}
export function addMessage(message, username, messageId, userInfo) {
  const elem = document.createElement('div');
  elem.innerHTML = `
<div class="message" id="m${messageId}">
    <div>${username}</div>
    <span>${message}</span>
</div>
`;
  const container = document.getElementById('main-container');
  if (container) {
    container.appendChild(elem);
  } else {
    console.error("#main-container not found in the DOM.");
  }
  console.log("Successfully appended message to the DOM " + message);
}